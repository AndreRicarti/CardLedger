version: "3"
services:
  api:
    build:
      context: ./invoice-api/CardLedger
      dockerfile: Dockerfile
    ports:
      - "7086:8080"
    volumes:
      - api-data:/app/data
    restart: unless-stopped

  frontend:
    build:
      context: ./invoice-dashboard
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  api-data:
